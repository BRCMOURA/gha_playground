on:
  push:
    branches:
      - main
      - master

jobs:
  meu-job:
    runs-on: ubuntu-latest

    steps:
      - run: |
          echo "Este job foi executado automaticamente por um evento."
          echo "job está rodando em um servidor hospedado pelo GitHub!"

  segundo-job:
    runs-on: ubuntu-latest

    steps:
      - run: |
          echo "Hoje é $(date)."
          ls -la

      - uses: actions/checkout@v6

      - run: ls -la
  
  
  