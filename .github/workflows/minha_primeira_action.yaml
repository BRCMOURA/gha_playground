on:
  push:
    branches:
      - main
      - master

jobs:
  meu-job:
    runs-on: ubuntu-latest

    steps:
      - run: echo "Este job foi executado automaticamente por um evento."
      - run: echo "job está rodando em um servidor hospedado pelo GitHub!"

  segundo-job:
      - run: echo "Hoje é $(date)."
  
  
  